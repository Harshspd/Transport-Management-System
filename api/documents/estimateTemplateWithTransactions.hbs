<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estimate</title>
    <style>
        /* Reset default margins and paddings */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .container {
            margin: auto;
            margin-top: 50px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .topmost {
            text-align: center;
            margin-bottom: 20px;
        }
        .estimate-title {
            font-size: 30px;
            font-weight: bold;
        }
        .account-info {
            width: 100%;
            margin-top: 20px;
            overflow: hidden;
        }
        .logo {
            float: left;
            max-width: 150px;
            height: auto;
            object-fit: contain;
        }
        .account-details {
            float: right;
            text-align: right;
            line-height: 1.6;
            font-size: 14px;
            width: calc(100% - 170px);
        }
        .middle-section table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        .middle-section th, .middle-section td {
            padding: 8px;
            vertical-align: top;
        }
        .middle-section .left {
            width: 43%;
            line-height: 1.5;
        }
        .middle-section .center, .middle-section .right {
            width: 25%;
        }
        .middle-section .center {
            text-align: center;
        }
        .middle-section .right {
            text-align: right;
        }
        .dist {
            margin-bottom: 5px;
        }
        .total-sec {
            text-align: right;
            margin-top: 15px;
        }
        hr {
            border: 0;
            border-top: 3px solid #000;
            margin-top: 20px;
        }
        .items-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 14px;
        }
        .items-table th, .items-table td {
            padding: 8px;
            text-align: right;
            vertical-align: top;
        }
        .items-table th.left-align, .items-table td.left-align {
            text-align: left;
        }
        .items-table tbody tr {
            border-bottom: 1px solid #ccc;
        }
        .item-description {
            font-weight: bold;
            font-size: 16px;
        }
        .bottom-estimate-sec {
            width: 100%;
            margin-top: 40px;
        }
        .bottom-estimate-sec table {
            width: 50%;
            float: right;
            border-collapse: collapse;
        }
        .bottom-estimate-sec th, .bottom-estimate-sec td {
            padding: 8px;
            text-align: left;
            vertical-align: top;
        }
        .bottom-estimate-sec th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .bottom-estimate-sec .total-label {
            text-align: right;
            font-weight: bold;
        }
        .bottom-estimate-sec .total-value {
            text-align: right;
            font-weight: bold;
        }
        .horizontal-line {
            background-color: #f2f2f2;
        }
        .horizontal-line td {
            padding: 1px;
            height: 1px;
        }
        .subtotal td, .discount td, .gst td {
            text-align: right;
        }
        .clear {
            clear: both;
        }
        .notes, .terms {
            margin-top: 20px;
            font-size: 14px;
        }
        .notes-title, .terms-title {
            font-weight: bold;
            margin-bottom: 10px;
            font-size: 16px;
        }
        .notes-content, .terms-content {
            margin-left: 20px;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <div class="topmost">
        <div class="estimate-title">Estimate</div>
    </div>
    <div class="account-info">
        <img src="{{estimate.account.logo}}" alt="Estimate Logo" class="logo" />
        <div class="account-details">
            <p>{{account.name}}</p>
            <p>{{account.address.address_line_1}}</p>
            <p>{{account.address.address_line_2}}</p>
            <p>{{account.address.city}}, {{account.address.state}}</p>
            <p>{{account.address.country_id.name}}, {{account.address.postal_code}}</p>
            <p>{{currency.name}}</p>
            <p>{{account.mobile}}</p>
            <p>GST:{{account.gstno}}</p>
            <p>PAN:{{account.panno}}</p>
            {{#if account.company_registeration_no}}
            <p>Registration No.:{{account.company_registeration_no}}</p>
            {{/if}}
        </div>
    </div>

    <div class="container">
        <div class="middle-section">
            <table>
                <tr>
                    <td class="left" >
                        <p><strong>For</strong></p>
                        <p>{{estimate.client.client_id.name}}</p>
                        <p>{{estimate.client.client_id.address.address_line_1}}</p>
                        <p>{{estimate.client.client_id.address.address_line_2}}</p>
                        <p>{{estimate.client.client_id.address.city}}, {{estimate.client.client_id.address.state}}</p>
                        <p>{{estimate.client.client_id.address.postal_code}}</p>
                        <p>{{estimate.client.client_id.address.country_id.name}}</p>
                        <p>GST: {{estimate.client.client_id.gstno}}</p>
                        <p>PAN: {{estimate.client.client_id.panno}}</p>
                        {{#if data.client.client_id.company_registeration_no}}
                        <p>Registration No.:{{invoice.client.client_id.company_registeration_no}}</p>
                        {{/if}}
                    </td>
                    <td class="center" style="width:40%">
                        <div class="date dist">
                            <p><strong>Date Issued</strong></p>
                            <p>{{estimate.issued_date}}</p>
                        </div>
                        <div class="est_no dist">
                            <p><strong>Estimate Number</strong></p>
                            <p>{{estimate.estimate_number}}</p>
                        </div>
                        {{#if estimate.reference_number}}
                        <div class="ref_no dist">
                            <p><strong>Reference</strong></p>
                            <p>{{estimate.reference_number}}</p>
                        </div>
                        {{/if}}
                    </td>
                    <td class="right">
                        <p><strong>Due Date</strong></p>
                        <p>{{estimate.due_date}}</p>
                    </td>
                </tr>
            </table>
        </div>
        <div class="total-sec dist">
            <p><strong>Estimated Total</strong></p>
            <p><strong>{{estimate.currency.currency}}&nbsp;&nbsp;{{formatFloat estimate.amount_due}}</strong></p>
        </div>
        <hr />
        <table class="items-table">
            <thead>
                <tr>
                    <th class="left-align">Item</th>
                    <th>HSN</th>
                    <th>Rate</th>
                    <th>Qty</th>  
                    <th>Amount</th>
                </tr>
            </thead>
            <tbody>
                {{#each estimate.items}}
                <tr>
                    <td class="left-align">
                        <span class="item-description">{{name}}</span><br />
                        {{description}}
                    </td>
                    <td>{{item.hsncode}}</td>
                    <td>{{estimate.currency.currency}}{{formatFloat price}}</td>
                    <td>{{quantity}}</td>
                    <td>{{estimate.currency.currency}}{{formatFloat line_total}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        <div class="bottom-estimate-sec">
            <table>
                <tr class="subtotal">
                    <td>Subtotal</td>
                    <td>{{formatFloat estimate.subtotal}}</td>
                </tr>
                {{#if estimate.discounts.length}}
                    {{#each estimate.discounts}}
                    <tr class="discount">
                        <td>{{name}} {{rate}}%</td>
                        <td>-{{formatFloat amount}}</td>
                    </tr>
                    {{/each}}
                {{/if}}
                {{#if estimate.taxes.length}}
                    {{#each estimate.taxes}}
                        <tr class="gst">
                            <td>{{name}}&nbsp;&nbsp;{{rate}}%</td>
                            <td>+{{formatFloat amount}}</td>
                        </tr>
                    {{/each}}
                {{/if}}
                <tr class="horizontal-line">
                    <td colspan="2"></td>
                </tr>
                <tr class="total">
                    <td class="total-label">Total</td>
                    <td class="total-value">{{estimate.currency.currency}}&nbsp;&pan;{{formatFloat estimate.total}}</td>
                </tr>
                 {{#if invoices.transactions}}
                {{#each invoice.transactions}}
                <tr class="deposit">
                    <td class="total-label">{{date}}&nbsp;{{description}}</td>
                    <td class="total-label">{{invoice.currency.currency}}&nbsp;&nbsp;{{formatFloat amount}}</td>
                </tr>
                {{/each}}
                {{/if}}
                <tr class="payment">
                    <td class="total-label">Payment</td>
                    <td class="total-value">{{estimate.currency.currency}}&nbsp;&nbsp;{{formatFloat estimate.total_payment}}</td>
                </tr>
            </table>
        </div>
        <div class="clear"></div>
        <div class="notes">
            <p class="notes-title">Notes</p>
            <p class="notes-content">{{estimate.notes}}</p>
        </div>
      
        <div class="terms">
            <p class="terms-title">Terms and Conditions</p>
            <p class="terms-content">{{estimate.terms}}</p>
        </div>
    </div>
</body>
</html>
